let initialState = {
    friends: [
        {id: 1, name: "Pasha"},
        {id: 2, name: "Serega"},
        {id: 3, name: "Kirill"}
    ]
};

const sidebarReducer = (state = initialState, action) => {

    return state;
};

export default sidebarReducer;